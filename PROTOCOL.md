# 难候选残余结构：最小化完备实验协议 v1.0

状态：实验设计，尚未执行。不得将本文件中的阈值、示例或预期表格写成实测结果。

## 0. 本次只检验的命题

在固定低成本初筛、固定候选集合和相同存储预算下，初筛后候选的残余差异是否包含可由**训练查询学到、在独立测试查询上复用**的局部线性结构；该结构能否比普通全局/局部降维减少完整距离计算，并在计入额外开销后获益。

这里的“难候选”采用操作性定义：RaBitQ 初筛保留、原本需要进一步精算的对象。它不等同于“理论上不可区分的对象”，也不要求其距离置信区间一定跨过查询半径。

本轮不改 GTS、不测试插入/删除、不调 gamma、不写新树、不训练 Transformer、不声称新颖性。否定结果只否定本协议中的固定分区、低秩线性探针，不否定所有高维检索或非线性表示方向。

## 1. 两个阶段，先小后大

- A：100K 数据规模的机制诊断。先完成正确性、独立查询泛化和字节预算对照。没有通过，不做新 CUDA 内核。
- B：仅当 A 有明确正面信号，做不使用真实第 k 距离的 GPU kNN 原型及 1M 扩展。

A 使用真实第 10 名距离作为固定半径，是带答案的诊断。A 的时间不能被宣传为可部署 kNN 时间。B 不得读取该半径。

## 2. 冻结输入与随机性

| 项目 | 规定 |
|---|---|
| 数据 | SIFT-128 与 GIST-960，均用原始数值转换得到的 FP32 L2 向量 |
| A 数据库规模 | 从各自 base 中用固定种子 20260920 抽取 100,000 个不同 ID |
| B 数据库规模 | 通过 A 后扩到 1,000,000；100K 必须是其子集；重新建立索引但不扩大方法/参数搜索空间 |
| 表示训练对象 | 从当前数据库固定抽取 32,768 个 ID；可用于中心、PCA 和无监督训练 |
| 查询 | 每个数据集固定选 1,000 条外部查询，按 256 train / 256 calibration / 488 test 划分 |
| 距离 | squared L2；所有半径、估计值、界和真值统一使用平方距离 |
| k | 10 |
| 最终质量目标 | 测得的 Recall@10 >= 0.999；不解释成所有未来查询的保证 |
| 初筛校准目标 | calibration CandidateRecall@10 >= 0.9995 |
| 随机实现种子 | 17、29、43；数据 ID 与查询划分保持相同，只改变模型/量化/聚类的随机初始化 |
| 局部区域 | 16 个 k-means 簇，仅用表示训练对象训练；之后固定每个数据库 ID 所属簇 |
| 公共投影维度 r0 | SIFT 16；GIST 64 |
| 追加秩 r | 16、32；不得看 test 后加其他秩 |

相同查询向量不得跨 train/calibration/test；先按数值相同的查询分组再划分。若无法满足目标数量，记录实际数量并在运行前修订协议，不拿 test 补训练。数据库中不同 ID 的重复向量保留，不删除不利样本。NaN/Inf 输入直接阻断运行；原始文件、选中 ID、查询划分均记录 SHA-256。

表示训练对象是建索引时已经可见的数据库内容，不是查询测试集。禁止用测试查询训练中心、方向、阈值、量化器或选择参数。

若只有经过 short 量化的旧 GTS 数据，必须把它标成独立输入版本，重新生成真值；不能混用原始浮点 benchmark 的真值。

## 3. 独立真值与同距

使用原始 FP32 输入值、FP64 的逐维差值平方累加，分块扫描全部数据库，生成 train/calibration/test 的 top-100 ID 与距离，另保存第 10 名平方距离 tau。

主要排序规则固定为 (squared_distance, stable_id)。距离相同仍视为不同对象，不以不同查询阶段的容差改变排序。边界同距查询单列占比及 tie-aware recall，主表仍使用固定 ID 规则。

所有候选排除使用严格的大于号；等于 tau 的对象不得仅凭边界相等被排除。不得通过移除同距查询来获得更漂亮的 recall。

真值计算默认 query block=8、database block=4096，不一次分配 N×Q×D 张量；真值只随数据/查询划分变化而重算，不随模型种子重算。小样本用第二个独立暴力实现交叉验证；向量维度、ID 映射、平方/非平方单位、重复 ID、输出去重都必须通过。GPU 近似结果或 gamma=1 的 GTS 结果不能充当独立真值。

## 4. 公共初筛 F0：使用成熟评分器，不用答案生成候选

使用作者维护的 RaBitQ-Library 中 1-bit RaBitQ 编码/距离估计组件，或与其逐项核对过的 cuVS 组件，锁定 commit 和具体路径。复用其中心、旋转、修正因子和真实存储格式，不把自行写的随机符号码称为 RaBitQ。

A 中对数据库所有对象进行低比特评分，不使用 IVF 少探簇、图搜索漏访问或真实距离来缩小候选宇宙。可以按 16 个固定簇组织编码，但所有簇都被评分。关闭隐藏的原始向量 rerank；独立记录 code_scored_count，必须等于 N。若接口不能导出这一过程，应写并验证评分组件适配器，而不是把接口返回的近邻当成全库评分。

按 (estimated_squared_distance, stable_id) 排序，保留前 M 个对象作为 C0(q)。M 从以下集合选择：

128, 256, 512, 1024, 2048, 4096, 8192, 16384, 32768, 65536, N。

超过 N 的值移除并去重。在 calibration 上选择达到初筛质量目标的最小 M。随后冻结；test 上不得逐查询扩大 M 或补入漏掉的真近邻。必须报告 M/N，若接近 1，说明当前初筛很弱，不能称作很强的过滤。

所有内部探针对同一 seed 使用字节一致的 C0(q)。第一阶段已有的漏检单独报告。后续方法不能找回未进入 C0 的对象，不能把其 recall 归一化到“已经在 C0 中的真近邻”。

## 5. 明确定义残余信息

以训练对象均值 mu 为公共中心，训练正交 PCA 基 P0，维度为 r0。

残余向量：e(x) = (I - P0 P0^T)(x - mu)。

训练对的残余差异：z(q,x) = e(q) - e(x)。

本协议所说的“残余”是公共 PCA 未表达的部分，**不是** RaBitQ 解码后的重构误差。RaBitQ 的作用是产生现实初筛候选；P0 的作用是让追加方向不只是重复捕获最明显的全局主方向。

各局部基 V_c 均投影到 P0 的正交补后再正交化。公共基、局部基、中心及每个对象的摘要必须在测试前写盘冻结。

## 6. 最少对照：不能只拿一个自制弱 baseline 比

| 组名 | 追加信息或执行方式 | 用途 |
|---|---|---|
| FULL | F0 后对 C0 全部算完整距离，再取 top-k | 基础工作量和时间对照，不是整个领域的最强 baseline |
| RQ+ | 把允许的追加存储交给更高比特 RaBitQ，而不是新子空间 | 排除“只是多花字节”的解释 |
| G | P0 之后普通全局 PCA 的后 r 个方向 | 排除普通增加维度即可获益 |
| L | 每簇数据库残余的普通局部 PCA | 排除普通局部低秩即可获益 |
| Q | 每簇训练查询与随机对象对的残余差异方向 | 排除普通 query-aware 表示即可获益 |
| H | 每簇训练查询与 C0 内候选对的残余差异方向 | 唯一待验证的 hard-conditioned 探针 |

G/L/Q/H 使用完全相同的摘要字段、判定公式、r、后备精算路径和校准规则，只改变方向来源。G 可只存一份共享 V，不强制复制 16 份来浪费它的内存。FULL 可以少用内存。

RQ+ 选用固定版本实际支持的位宽，在预算内做 calibration 选择；不要凭“每维多少 bit”忽略修正因子、对齐与中心。当前 cuVS 文档列出的总位宽为 1..9；具体运行以所锁定版本为准。

RQ+ 在相同 C0 上用更多位重新评分，再保留 M1 个对象精算。M1 从 10、32、64、128、256、512、1024、2048、4096、M 中取不超过 M 的值；用 calibration 选满足质量要求的最小值。它不是区间判定器，不应给它编造 certified ambiguous count。另在 B 中加入原生多位 RaBitQ 的完整查询，不能只比较被公共 F0 束缚的组件版本。

## 7. H/Q 的训练细节：共享，而不是每个测试查询现拟合

H：对每个 train 查询，从 C0(q) 用固定哈希抽取至多 256 个对象；不根据真实距离、正负标签或实际错误筛选。保存 query_id、candidate_id、cluster_id。

Q：对每个 train 查询和每个簇，随机抽取与 H 在该 (query, cluster) 中同样数量的数据库对象。与 H 保持相同查询、相同每簇样本数和同样训练规模；允许偶然抽到 H 的候选，不人为排除。

对每个非零 z，形成 u=z/||z||。同一查询所有有效训练对的权重之和为 1。按簇累积 S_c = sum w*u*u^T，取前 r 个特征方向；使用不中心化二阶矩，因为这里学习的是差异方向。z=0 的对训练时不参与方向估计，但必须保留在查询评估中并报告数量。

L：对各簇训练数据库对象的 e(x) 做常规中心化 PCA。G：取公共全局 PCA 的后 r 个方向。

若某簇 H 或 Q 少于 4*r 个非零训练对，两组在该簇都回退到同一 G 基；记录训练稀疏簇和测试使用比例，不用 test 补样本。

所有模型只用 train 拟合。r 和 RQ+ 位宽/保留数只用 calibration 选择。3 个随机种子不是 3 个独立数据库，也不能被当作额外独立测试查询。

## 8. 摘要、判定与完整精算

对属于簇 c 的数据库对象，预存：

- a_x = P0^T(x-mu)；
- b_x = V_c^T e(x)；
- s_x = ||e(x)-V_c b_x||；
- 原始中心化向量的平方范数（用于数值保护/审计）。

每条查询只计算一次公共 a_q，并为其候选出现的每个簇计算一次 b_q,c 和 s_q,c。不得为每个候选读取原始 x 重算摘要。

令 t = ||a_q-a_x||^2 + ||b_q,c-b_x||^2，则实数运算下：

L2 = t + (s_q,c-s_x)^2；
U2 = t + (s_q,c+s_x)^2。

由正交分解和三角不等式可得 L2 <= ||q-x||^2 <= U2。这只是本实验的标准判定工具，不是新颖性主张。

A 的固定 tau 模式：L2 > tau 才排除；其余对象均交给完整距离计算。U2 <= tau 的对象可计作“已知在范围内”，但由于最终仍要 top-k 排序，本协议仍精算它，不虚报成完全免费。

A 同时记录“区间跨阈值”与“仍需完整距离”的数量：这两者不同。

数值要求：方向学习和参考计算用 FP64；主表摘要按 FP32 实际序列化后读回测量，不能用 FP64 效果配 FP32 字节数。基矩阵也按 FP32 序列化；数据库摘要与查询变换必须使用同一份读回的模型，不能混用训练时 FP64 基与运行时 FP32 基。近似正交性、摘要舍入和范数相减都可能影响界。主结果须同时提供 FP64 参考与实际 FP32 摘要结果。预声明 guard = 1e-6*(1+||q-mu||^2+||x-mu||^2)，仅在 L2 > tau+guard 时排除；guard 内、负残余范数超过舍入范围、NaN/Inf、无法可靠判定者全部回退精算。此 guard 是固定实验数值策略，**不是已证明的通用误差界**。

离线用全维 FP64 真值审计所有被排除的 C0 对象，要求本轮新增误剪数为 0。若不为 0，该配置判为数值/正确性失败，不能调大 guard 后覆写同一轮；修复须新版本重跑。有限测试零错误不等于最坏情况精确保证。B 如要声称 certified pruning，必须另完成向外舍入或可证明误差传播；未完成只能报告测得质量。

## 9. 相同存储预算

所有配置都保留同一原始 FP32 数据库，计入公共成本；不得让某组使用磁盘原始向量、另一组使用显存原始向量而不披露。

令 common 为 F0 的实际编码、中心、旋转、修正参数、ID/簇归属等所有持久结构。对每个 r，追加预算在运行前固定为：

B_extra(r) = 4*N*(r0+r+2) + 4*D*r0 + 4*16*D*r + 4*D 字节。

该式是 FP32 摘要与模型的逻辑预算，不自动等于 allocator 的实际保留量。实际分配、padding、序列化文件、向上舍入容量全部另外记录，超出预算则配置不合格，不得只报告理论码长。若实现需要额外误差界字段，也计入预算，不能免费增加。

RQ+ 可在 common+B_extra 的总上限内使用更好的编码；支持删除不需要的旧码副本，但不允许计时时同时驻留而内存表只算一份。

同时记录 CPU 持久字节、GPU 持久 used/reserved、峰值 workspace、离线训练时间、query transform 工作量。共享模型不能当作零成本；GPU profiler 未测得的流量只能称“逻辑请求字节”，不能称实际 DRAM 流量。

## 10. A 必须输出的机制指标

所有决策在完整 C0 上评估；只允许对可视化/谱分析做固定抽样，不能抽样后称为完整查询效果。

1. CandidateRecall@10：第一阶段保留的真实 top-10 比例，及 zero-recall 查询比例。
2. 每查询 C0 大小、需要全维精算的数量及 P50/P95，FP32 新增误剪数。
3. 难负例保留数：x 在 C0 且真实距离 > tau，经过追加判定后仍保留的数量。真实标签只供事后统计。
4. 测试对能量覆盖 E_r=||V_c^T z||^2/||z||^2；报告 train/calibration/test 的均值和分位数，不只报告训练谱。
5. 三个真值距离分层：d2<=tau；tau<d2<=1.1*tau；d2>1.1*tau。tau=0 单独列出。不得仅挑远处负例。
6. 计入全部成本后的逻辑读取字节，以及实际摘要/模型字节。

H 对最强内部控制 G/L/Q 的新增过滤率定义为 1 - (H 的难负例保留总数)/(控制组难负例保留总数)。控制组难负例保留数为零时，该比值记 NA，不能宣称胜出。不能只比较总候选中巨量的容易负例，也不能把真近邻减少算作收益。

至少保存 per_query.csv、summary.json、候选集合哈希、模型哈希与训练对列表。置信区间以查询为配对单位 bootstrap 2,000 次；3 个模型种子的结果单列，不将重复运行当作独立样本。

可选且只能在主结果锁定之后做一次 test-fit 诊断：用 test 的困难对拟合同秩局部方向，看能量与过滤潜力是否明显提高。必须标 ORACLE_ONLY，不参与性能排名、go 判定或超参选择。PCA 的能量最优不等于剪枝最优，该诊断不是任意算法的上界。

## 11. B：从带答案诊断变成真实查询

仅 A 通过后做。用 calibration 选择每个方法在预算内的配置，然后冻结。

内部 G/L/Q/H 的真实查询：F0 生成 C0；先按 F0 分数取前 min(64,M) 个不同 ID 算完整距离，形成真实当前 top-10 阈值；剩余候选按固定 (cluster_id, F0_rank, stable_id) 排序，每 128 个为一块。每块先算摘要下界，只精算未排除对象，合并 top-k 后再进入下一块。等距 ID 规则与真值一致。所有内部方法使用同样顺序和分块。

FULL 对 C0 使用优化的批量精算与 top-k，不故意强迫它逐块反馈造成额外开销。RQ+ 使用已冻结位宽和 M1。B 还必须包括：原生高位 RaBitQ 与作者 LoRANN/RRR 评分组件的同预算高召回对照；组件比较固定 C0，整系统比较各走原生路径并分表。不能把普通局部 PCA 当成 LoRANN 的完整替代。缺失这些直接先例的对照时，只能报告内部机制结果，不可进入“优于成熟表示”的结论。

B 的检索路径禁止读取任何 ground-truth ID/tau；真值文件仅对进程结束后的验证器可见。初始 64 个对象的完整计算必须计费。不能把前序测试查询真实近邻当作免费的 warm start。

主要 batch=128；batch=1 只作成本诊断，不事后更换主要目标。计时覆盖 F0、query transform、summary gather、界计算、full-distance gather、精算和 top-k；分别记录 wall time 与 CUDA events。原始数据预驻留，建索引/首次 H2D/冷启动另外报告，不藏在 hot-query 结果里。

每 seed 三个新进程；每进程预热 5 轮、计时 20 轮完整测试集；方法执行顺序轮换。禁止每次查询重新申请大工作区，所有方案允许合理复用。固定 GPU UUID、软件版本与功耗/共享负载状态；内存不足直接记录 OOM，不改小数据偷偷续跑。至少一次 sanitizer 验证。重复回放不是更多独立查询，不据其推断生产 p99。

## 12. 运行前固定的决策规则

下列 20%/10%/15% 是本轮投入门槛，不是科学规律，不得看 test 后修改。

### A-GO：值得做小型 GPU 原型

在同一数据集、同一预算下，H 满足：

- FP32 摘要没有新增误剪，最终 Recall@10>=0.999；
- 比 calibration 选定的最强 G/L/Q 控制少至少 20% 的难负例完整精算；三个 seed 至少两个达到，配对查询区间报告出来；
- 计入 query transform 与模型后，逻辑读取字节至少少 10%；实际持久存储不超预算；
- 若 RQ+ 在质量、精算量和字节数上全面不差于 H，则不进入新内核开发，应优先保留成熟表示。

只在一个数据集通过，标为 workload-specific 线索；两数据集都通过，才可称跨这两种数据的信号。

### B-GO：值得进入新颖性与论文立项评估

在 1M、batch128、测得 Recall@10>=0.999、同预算下，相比 calibration 选定的最佳可比成熟基线，H 完整查询中位时间至少下降 15%，且每 seed 的三个进程配对都更快；保留所有种子、失败与反例。最终测试若质量未达标，该点淘汰，不能边看 test 边增加 M/M1。

A 通过、B 不通过：可复用结构有迹象，但系统收益未建立。H 只胜 G 而不胜 L/Q：普通局部性或 query-aware 表示即可解释，不支持 hard-conditioning 的独立贡献。训练好测试差：不可部署的过拟合。所有线性探针失败：只否定本轮具体线性路线。全部通过也不等于 novelty 已证实，需继续对 DADE、LoRANN、RaBitQ 等做机制级差异核查。

## 13. 最小交付物与执行顺序

建议文件结构（这是待实现接口，不表示这些脚本已经存在）：

```
experiment/
  PROTOCOL.md
  config.yaml
  environment.json
  source_pins.json
  split_ids.npz
  truth/
  models/
  candidates/
  train_pairs/
  results/per_query.csv
  results/memory.csv
  results/timing.csv
  results/summary.json
  results/DECISION.md
```

按 prepare -> truth -> front_calibrate -> candidate_dump -> fit_probes -> calibration -> freeze -> test -> verify -> decide 执行。B 独立运行，不复用 A 的真实半径。

预算：前 1–2 天完成输入、真值和评分器适配；第 3–4 天完成 A。若成熟组件无法正确接入，标 BLOCKED，不用弱替代品制造结论。只有 A-GO 才安排最多 3–5 天的 B；到期把未测项标 UNKNOWN，不增添新数据集、模型或动态更新任务。

最终 DECISION.md 必须分别回答：是否有训练外可复用结构、是否减少决策所需的信息、是否优于同预算普通/成熟方法、是否带来完整查询收益。四个问题不能用一个“成功/失败”混在一起。

## 14. 来源与先例边界

本文件的参数、分组和 go/no-go 阈值是本轮提案，不是文献或旧实验的结果。

[1] Gao & Long, RaBitQ, SIGMOD 2024. https://arxiv.org/abs/2405.12497
[2] RaBitQ 作者维护库。https://github.com/VectorDB-NTU/RaBitQ-Library
[3] NVIDIA cuVS IVF-RaBitQ 参数文档（需运行时锁定版本）。https://docs.nvidia.com/cuvs/api-reference/cpp-api-neighbors-ivf-rabitq
[4] Jääsaari et al., LoRANN: Low-Rank Matrix Factorization for Approximate Nearest Neighbor Search. https://arxiv.org/abs/2410.18926
[5] Deng et al., Efficient Data-aware Distance Comparison Operations for High-Dimensional Approximate Nearest Neighbor Search (DADE), PVLDB 18(3). https://www.vldb.org/pvldb/vol18/p812-zheng.pdf
[6] Gao & Long, High-Dimensional Approximate Nearest Neighbor Search: with Reliable and Efficient Distance Comparison Operations (ADSampling). https://arxiv.org/abs/2303.09855
[7] Shi et al., GPU-Native Approximate Nearest Neighbor Search with IVF-RaBitQ: Fast Index Build and Search. https://arxiv.org/abs/2602.23999

ADSampling 已研究避免多数完整维度比较；DADE 已研究数据感知距离估计；LoRANN 已研究监督局部低秩评分；GPU IVF-RaBitQ 已有低比特与融合执行。普通投影、渐进精化、查询感知训练和 GPU 合批本身均不作为本轮新颖性主张。
